<p style="color: green"><%= notice %></p>

<%= render @journal %>

<div>
  <%= link_to "Edit this journal", edit_journal_path(@user.id, @journal.id) %> |
  <%= link_to "Back to journals", user_journals_path(@user.id) %>

  <%= button_to "Destroy this journal", @journal, method: :delete %>
</div>
